import React from 'react';
import ReactDOM from 'react-dom/client';
import { HashRouter } from 'react-router-dom';
import App from './App';
import './index.css';

// HashRouter is used instead of BrowserRouter so routing works correctly
// when the built app is loaded from the file:// protocol in Electron and
// in mobile WebViews via Capacitor, not just from an HTTP server.
ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <HashRouter>
      <App />
    </HashRouter>
  </React.StrictMode>
);
